"github", "LA-WLF" | ForEach-Object {
    $variableName = "NuGetPackageSourceCredentials_$_"

    [pscustomobject]@{
        Name    = $variableName
        Process = [bool][Environment]::GetEnvironmentVariable(
            $variableName,
            "Process"
        )
        User    = [bool][Environment]::GetEnvironmentVariable(
            $variableName,
            "User"
        )
        Machine = [bool][Environment]::GetEnvironmentVariable(
            $variableName,
            "Machine"
        )
    }
}


To remove old values from the current PowerShell session:

Remove-Item Env:NuGetPackageSourceCredentials_github `
    -ErrorAction SilentlyContinue

Remove-Item Env:NuGetPackageSourceCredentials_LA-WLF `
    -ErrorAction SilentlyContinue

If the previous table showed a user-level value, remove it permanently:

[Environment]::SetEnvironmentVariable(
    "NuGetPackageSourceCredentials_github",
    $null,
    "User"
)

[Environment]::SetEnvironmentVariable(
    "NuGetPackageSourceCredentials_LA-WLF",
    $null,
    "User"
)


3. Force NuGet to use the exact configuration file you edited

Run:

$configFile = Join-Path $env:APPDATA "NuGet\NuGet.Config"

dotnet nuget list source `
    --configfile $configFile `
    --format Detailed

Then:

dotnet restore `
    --configfile $configFile `
    --no-cache `
    --verbosity normal
    
    
    
The package might belong to another repository
The application repository is:

Wlf.Mp.Api

The package is:

Wlf.Identity.IntegrationEvents

That package may be:
	• Linked to a different Identity repository 
	• Configured with its own user/team access list 
	• Linked to a repository but not inheriting its permissions 
	• Inheriting from a repository you cannot access 

NuGet packages can either inherit permission from a linked repository or use independently configured permissions. 

The package administrator needs to inspect:
	LA-WLF
→ Packages
→ Wlf.Identity.IntegrationEvents
→ Package settings
→ Manage access or Inherited access

They should determine:
	1. Which repository, if any, is linked to the package. 
	2. Whether Inherit access from repository is enabled. 
Whether your GitHub username or team has Read access.


Get-ChildItem -Recurse -File -Include `
    *.csproj,Directory.Packages.props,Directory.Build.props,*.props,*.targets |
    Select-String -Pattern `
        "Wlf.Identity.IntegrationEvents|Serilog.AspNetCore"
        
/////////////////////////////////////////////
//
//
The API cannot completely restore because it eventually reaches:

Wlf.Identity.IntegrationEvents

and GitHub Packages rejects that private-package request with 403 Forbidden. Public dependencies may still download successfully, but one unavailable dependency causes the project or solution restore to be considered failed.


/////////////////////////////////////////////////////////////

For Wlf.Identity.IntegrationEvents, I need two things:

1. The Git repository containing the source code
2. The compiled NuGet package published to GitHub Packages

Those are separately stored and separately authorized.


Git source repository
github.com/LA-WLF/<identity-repository>
	git clone succeeds

NuGet package
nuget.pkg.github.com/LA-WLF/
Wlf.Identity.IntegrationEvents
	dotnet restore receives 403
	
When GitHub returns 403 Forbidden for Wlf.Identity.IntegrationEvents
	that means the request to download the published NuGet package, not the request to clone its source-code repository.


//What one owner should do

They should sign into GitHub and navigate to:

LA-WLF organization
→ Packages
→ Wlf.Identity.IntegrationEvents
→ Package settings

On the package settings page, they should find either Manage access or Inherited access. GitHub uses different wording depending on whether package permissions are independent or inherited from a repository.

If it says “Manage access”

The package has its own permission list.

If it says “Inherited access”

The package inherits permission from a linked repository.

The owner should identify which repository is linked to the package. If the package is supposed to inherit from the repository containing the Wlf.Identity.IntegrationEvents source code, they should verify:

Inherit access from repository: enabled
Linked repository: the correct Identity repository
Your account/team: Read access to that repository

GitHub says a user with Read access to a linked repository should receive Read access to the package when inheritance is enabled.


///////////////////////////////////////////////
//
//  this is probably what happened to Wlf.Identity.IntegrationEvents 

The package is linked to a different repository.
The package was linked after it was published and inheritance was never enabled.
The organization disabled automatic inheritance.
The package uses independent permissions despite appearing related to the repository.


	
// Message to send to Owners of the LA-WLF GitHub organization
Please open LA-WLF -> Packages -> Wlf.Identity.IntegrationEvents -> Package settings. 

Under Manage access or Inherited access, verify that my GitHub username (labswal) or the UI Team has Read permission.

If the package inherits permissions, please verify that it is linked to the correct Identity repository and that Inherit access from repository is enabled. This is package access for local NuGet restore, not Manage Actions access.



//////////////////////////////////////////////////////////
//
//  This might be a local problem (me)
//  

$credential = Get-Credential `
    -UserName "YOUR_PERSONAL_GITHUB_USERNAME" `
    -Message "Enter your classic GitHub PAT as the password"

$credentialText = "{0}:{1}" -f `
    $credential.UserName,
    $credential.GetNetworkCredential().Password

$authorization = [Convert]::ToBase64String(
    [Text.Encoding]::ASCII.GetBytes($credentialText)
)

Invoke-WebRequest `
    -Uri "https://nuget.pkg.github.com/LA-WLF/index.json" `
    -Headers @{
        Authorization = "Basic $authorization"
        Accept        = "application/json"
    }
    
////////////////////////////////////////////////////

Invoke-WebRequest `
    -UseBasicParsing `
    -Uri "https://nuget.pkg.github.com/LA-WLF/download/wlf.mp.common.application/index.json" `
    -Headers @{
        Authorization = "Basic $authorization"
        Accept        = "application/json"
    }
    
Invoke-WebRequest `
    -UseBasicParsing `
    -Uri "https://nuget.pkg.github.com/LA-WLF/download/wlf.identity.integrationevents/index.json" `
    -Headers @{
        Authorization = "Basic $authorization"
        Accept        = "application/json"
    }
    
These tests above PROVE that permissions are ok.

The exact endpoint for Wlf.Identity.IntegrationEvents returned HTTP 200 and listed versions 1.0.4 and 1.0.0. Together with the successful Wlf.Mp.Common.Application test, this proves:

Your classic PAT works.
SSO authorization works.
Your account can read both packages.
The corporate proxy is passing the authenticated requests.
No organization owner needs to change package permissions

//////////////////////////////////////////////////

$githubUsername = "labswal"
$pat = $credential.GetNetworkCredential().Password



$credentialValue =
    "Username=$githubUsername;" +
    "Password=$pat;" +
    "ValidAuthenticationTypes=Basic"

[Environment]::SetEnvironmentVariable(
    "NuGetPackageSourceCredentials_LA-WLF",
    $credentialValue,
    "Process"
)



[Environment]::GetEnvironmentVariable(
    "NuGetPackageSourceCredentials_LA-WLF",
    "Process"
) -ne $null



$configFile = Join-Path $env:APPDATA "NuGet\NuGet.Config"

dotnet nuget disable source "github" `
    --configfile $configFile
    
    
cd "C:\path\to\Wlf.Mp.IdentityServer"

dotnet restore `
    --configfile $configFile `
    --no-cache `
    --verbosity normal